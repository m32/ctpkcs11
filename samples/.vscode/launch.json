{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current File",
      "type": "python",
      "request": "launch",
      "justMyCode": false,
      "cwd": "${workspaceFolder}",
      "program": "${file}"
    }
  ]
}